# Backend env example
# Copy this file to backend/.env and fill values. Do NOT commit secrets.

# Server
PORT=5000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_KEY=your_supabase_service_key

# Optional: Alibaba Cloud Bailian (通义千问) API key for AI text-generation.
# You can either pass the key from the frontend (not recommended for production)
# or store it here (recommended) or in your deployment secrets (GitHub/GCP/Azure/kv).
BAILIAN_API_KEY=your-bailian-api-key
# Alternate name supported by the code
ALIYUN_API_KEY=your-aliyun-api-key
